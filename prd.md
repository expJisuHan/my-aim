# PRD — 나만의 AIm

AI 기반 게이미피케이션 목표 실행 MVP

---

## 1. 서비스 한 줄 설명

나만의 AIm은 사용자가 해야 할 일 하나만 입력하면, AI가 이를 짧은 미션 단위로 쪼개고 듀오링고처럼 단계별로 진행하게 해주는 목표 실행 프로그램이다.

사용자는 계획을 직접 짜지 않아도 되고, 각 미션을 시작하면 타이머가 작동하며 완료 시 포인트와 보상을 받는다.

---

## 2. MVP 핵심 방향

**MVP 목표**

복잡한 계획표 앱이 아니라, "시작 못 하는 문제"를 해결하는 실행 보조 앱으로 만든다.

사용자가 할 일 하나를 입력하면:

1. AI가 목표를 작은 단계로 나눔
2. 단계가 듀오링고 챕터 UI처럼 표시됨
3. 사용자가 미션을 하나씩 시작함
4. 타이머가 켜짐
5. 완료하면 점수 지급
6. 일정 시간 동안 시작하지 않거나 오래 걸리면 알림 제공
7. 하루 진행량에 따라 보너스 지급

---

## 3. 문제 정의

**사용자가 겪는 문제**

| 문제 | 설명 |
|------|------|
| 시작 장벽 | 해야 하는 건 알지만 첫 행동을 시작하지 못함 |
| 계획 부담 | 큰 목표를 어떻게 나눠야 할지 몰라서 미룸 |
| 흥미 저하 | 투두리스트는 완료 체크 외에 재미 요소가 부족함 |
| 실패 부담 | 하루 실패하면 전체 목표를 포기하는 경우가 많음 |
| 지속 어려움 | 개인 의지만으로 장기 목표를 유지하기 어려움 |

---

## 4. 해결 방향

| 문제 | 해결 방식 |
|------|-----------|
| 시작 장벽 | 목표를 아주 작은 첫 미션으로 쪼갬 |
| 계획 부담 | AI가 자동으로 챕터와 미션 생성 |
| 흥미 저하 | 경험치, 포인트, 레벨, 연속 진행 보상 제공 |
| 실패 부담 | 실패 감점 없음, 돌아오면 복귀 보상 제공 |
| 지속 어려움 | 커뮤니티는 MVP에서는 목업 처리, 추후 확장 |

---

## 5. MVP 범위

**포함할 기능**

| 구분 | 기능 |
|------|------|
| 목표 입력 | 사용자가 해야 할 일/목표를 한 문장으로 입력 |
| AI 세부화 | LLM API로 목표를 챕터/미션 단위로 분해 |
| 단계형 UI | 듀오링고처럼 챕터 노드 형태로 미션 표시 |
| 미션 실행 | 미션 시작 버튼 클릭 시 타이머 시작 |
| 미션 완료 | 완료 버튼 클릭 시 포인트 지급 |
| 시간 알림 | 30분 이상 진행 시 팝업/토스트 알림 |
| 미시작 알림 | 생성 후 시작하지 않으면 30분 단위로 리마인드 |
| 점수 시스템 | 기본 점수, 하루 보너스, 추가 미션 보상 |
| 진행률 | 오늘 진행률, 전체 목표 진행률 표시 |
| AI 피드백 | 하루 완료 후 짧은 피드백 제공 |
| 커뮤니티 | 실제 기능 제외, "준비 중" 목업 화면만 제공 |

**제외할 기능**

| 기능 | MVP 제외 이유 |
|------|--------------|
| 실제 커뮤니티 매칭 | 구현 비용 큼 |
| 실시간 채팅 | 핵심 가치와 직접 관련 낮음 |
| 친구/그룹 인증 | 후순위 |
| 복잡한 캘린더 | 초기 프로토타입에서는 부담 |
| 모바일 앱 푸시 | 웹 MVP에서는 브라우저 알림 또는 토스트로 대체 |
| 회원가입 고도화 | 간단 로그인 또는 로컬 저장으로 시작 가능 |

---

## 6. 타깃 사용자

**1차 타깃**

해야 할 일은 있지만 시작을 계속 미루는 대학생/취준생/직장인

**대표 사용자 시나리오**

> "내일까지 발표 자료를 만들어야 하는데 뭐부터 해야 할지 모르겠고, 시작하기가 너무 부담스럽다."

사용자가 입력: `창업 아이디어 발표자료 만들기`

AI가 변환:
1. 발표 주제 한 줄로 정리하기
2. 문제 정의 3줄 작성하기
3. 해결 아이디어 정리하기
4. 핵심 기능 3개 뽑기
5. 슬라이드 목차 만들기
6. 첫 번째 슬라이드 초안 만들기

사용자는 각 미션을 하나씩 완료하면서 점수와 진행률을 얻는다.

---

## 7. 핵심 사용자 플로우

```
사용자 목표 입력
  → AI가 목표 분석
  → 챕터/미션 자동 생성
  → 듀오링고식 진행맵 표시
  → 사용자가 첫 미션 시작
  → 타이머 시작
  → 미션 완료? 
      완료 → 포인트 지급 → 다음 미션 해금
               → 오늘 목표량 달성? 
                   달성 → 보너스 포인트 지급
                   미달성 → 다음 미션 추천
      미완료(30분 경과) → 시간 알림 팝업
  → 30분간 시작 안 함 → 시작 리마인드 알림
```

---

## 8. 화면 구성 IA

```
Home
├── Goal Input
│   └── AI Plan Loading
│       └── Mission Map
│           ├── Mission Detail
│           │   └── Running Timer
│           │       └── Mission Complete
│           └── Daily Result
├── My Progress
└── Community Mock
```

---

## 9. 주요 화면 설계

### 9-1. Home

**목적:** 사용자가 바로 목표를 입력하게 한다.

**구성**
- 서비스 로고: 나만의 AIm
- 문구: "해야 할 일 하나만 적어보세요. AI가 바로 시작 가능한 단계로 나눠드려요."
- 목표 입력창
- 예시 버튼: 과제 시작하기 / 발표 준비하기 / 시험 공부하기 / 포트폴리오 만들기
- CTA 버튼: AI로 미션 만들기

---

### 9-2. Goal Input

**입력 예시:** `내일까지 창업 아이디어 발표자료 만들기`

| 항목 | 필수 여부 | 설명 |
|------|-----------|------|
| 목표 제목 | 필수 | 사용자가 해야 할 일 |
| 마감일 | 선택 | 없으면 오늘 기준으로 생성 |
| 난이도 | 선택 | 쉬움/보통/빡셈 |
| 하루 목표 미션 수 | 선택 | 기본 3개 |

MVP에서는 목표 제목만 받아도 작동하도록 만든다.

---

### 9-3. AI Plan Loading

**화면 문구**

> AI가 목표를 잘게 나누고 있어요.  
> 첫 미션은 부담 없게 시작할 수 있도록 만들게요.

**로딩 중 표시**
- 진행 애니메이션
- "목표 분석 중"
- "챕터 나누는 중"
- "오늘 할 미션 정리 중"

---

### 9-4. Mission Map

**핵심 UX:** 듀오링고처럼 원형 노드가 이어진 단계형 맵으로 구성한다.

**상태**

| 상태 | UI |
|------|----|
| 완료 | 초록 체크 노드 |
| 진행 가능 | 강조된 노드 |
| 잠김 | 회색 잠금 노드 |
| 진행 중 | 타이머 아이콘 노드 |

**예시**

```
Chapter 1. 시작하기
● 미션 1: 발표 주제 한 줄 정리하기
● 미션 2: 문제 상황 3줄 작성하기
● 미션 3: 해결 아이디어 적기

Chapter 2. 구조 잡기
🔒 미션 4: 슬라이드 목차 만들기
🔒 미션 5: 핵심 기능 정리하기
```

---

### 9-5. Mission Detail

**구성**
- 미션 제목
- 예상 소요 시간
- 미션 설명
- 완료 기준
- 시작 버튼

**예시**

```
미션 1
발표 주제 한 줄 정리하기
예상 시간: 5분

해야 할 일:
지금 만들고 싶은 서비스를 한 문장으로 적어보세요.

완료 기준:
"누가, 어떤 문제를, 어떻게 해결하는지"가 들어간 한 문장을 작성하면 완료입니다.

[미션 시작하기]
```

---

### 9-6. Running Timer

**구성**
- 진행 중인 미션 제목
- 스톱워치
- 일시정지 버튼
- 포기하지 않기 문구
- 완료 버튼

**30분 초과 시 팝업**

> 벌써 30분이 지났어요.  
> 이 미션이 너무 크다면 더 작게 나눠볼까요?  
> [계속 진행하기] [AI에게 더 쪼개달라고 하기]

MVP에서는 [AI에게 더 쪼개달라고 하기] 버튼을 누르면 같은 미션을 2~3개 하위 미션으로 재생성한다.

---

### 9-7. Mission Complete

**완료 시 화면**

```
미션 완료!
+5pt 획득
오늘 2/3개 미션 완료
하나만 더 하면 오늘 보너스 +30pt!
```

**버튼**
- 다음 미션 하기
- 오늘 결과 보기

---

### 9-8. Daily Result

**하루 목표량 달성 시**

```
오늘의 목표 달성!
기본 미션 3개 완료
보너스 +30pt 획득
총 획득 포인트: 45pt
```

**AI 피드백 예시**

> 오늘은 시작 장벽을 넘긴 게 가장 큰 성과예요.  
> 내일은 첫 미션을 더 짧게 설정하면 더 쉽게 이어갈 수 있어요.

---

## 10. 기능 요구사항

### FR-1. 목표 입력

**설명:** 사용자가 해야 할 일을 한 문장으로 입력한다.

**조건**
- 최소 3자 이상
- 비어 있으면 생성 불가
- 마감일은 선택값
- 목표 생성 시 기본 상태는 `active`

---

### FR-2. AI 목표 세부화

**설명:** LLM API를 사용해 목표를 챕터와 미션으로 나눈다.

**생성 기준**
- 미션은 5~20분 안에 수행 가능해야 함
- 첫 미션은 가장 쉬워야 함
- 각 미션에는 완료 기준이 있어야 함
- 전체 목표는 2~4개 챕터로 나눔
- MVP에서는 총 6~12개 미션 생성

**LLM 출력 JSON 예시**

```json
{
  "goalTitle": "창업 아이디어 발표자료 만들기",
  "chapters": [
    {
      "title": "아이디어 정리",
      "missions": [
        {
          "title": "서비스 한 줄 설명 작성하기",
          "description": "서비스가 누구의 어떤 문제를 해결하는지 한 문장으로 작성한다.",
          "estimatedMinutes": 5,
          "completionCriteria": "한 문장 설명이 작성되면 완료"
        },
        {
          "title": "문제 상황 3줄 정리하기",
          "description": "사용자가 겪는 문제를 3줄로 정리한다.",
          "estimatedMinutes": 10,
          "completionCriteria": "문제 3개 이상 작성하면 완료"
        }
      ]
    }
  ]
}
```

---

### FR-3. 챕터형 미션 맵

**설명:** AI가 생성한 미션을 듀오링고식 단계 UI로 보여준다.

**규칙**
- 첫 번째 미션만 처음부터 열려 있음
- 이전 미션을 완료하면 다음 미션 해금
- 완료한 미션은 체크 표시
- 잠긴 미션은 흐리게 표시
- 오늘 추천 미션은 강조 표시

---

### FR-4. 미션 시작

**설명:** 사용자가 미션을 시작하면 타이머가 작동한다.

**조건**
- 시작 버튼 클릭 시 `mission.status = in_progress`
- `startedAt` 저장
- 타이머는 초 단위로 표시
- 미션 실행 중 새로고침해도 진행 상태 유지 (MVP에서는 localStorage 저장)

---

### FR-5. 미션 완료

**설명:** 사용자가 완료 버튼을 누르면 미션이 완료 처리되고 포인트가 지급된다.

**조건**
- `completedAt` 저장
- `durationSeconds` 저장
- 포인트 계산
- 다음 미션 해금
- 오늘 완료 미션 수 업데이트

---

### FR-6. 포인트 시스템

**기본 정책**

| 조건 | 포인트 |
|------|--------|
| 하루 기본 미션 1개 완료 | +5pt |
| 하루 목표 미션 수 달성 | +30pt |
| 하루 목표량 초과 후 추가 미션 완료 | 미션당 +7pt |
| 중단 후 다시 돌아와 완료 | +3pt 복귀 보상 |
| 실패/미완료 | 감점 없음 |

**예시** (하루 목표 미션 수가 3개인 경우)

- 1번째 미션 완료: +5pt
- 2번째 미션 완료: +5pt
- 3번째 미션 완료: +5pt + 보너스 30pt
- 4번째 미션 완료: +7pt

---

### FR-7. 30분 시간 알림

**케이스 1. 미션 진행 중 30분 초과**

> 이 미션을 시작한 지 30분이 지났어요.  
> 계속 진행할까요, 아니면 더 작게 나눠볼까요?

**케이스 2. 미션 생성 후 시작하지 않음**

> 첫 미션은 5분이면 시작할 수 있어요.  
> 지금 바로 시작해볼까요?

**MVP 구현 방식**

| 방식 | 설명 |
|------|------|
| 브라우저 열려 있음 | setInterval로 30분 후 모달, 예상 시간 경과 시 toast + beep |
| 브라우저 알림 허용 | Notification API 사용 (미구현, 추후 확장) |
| 브라우저 닫힘 | MVP에서는 제외 또는 추후 푸시 알림으로 확장 |

**추가 구현: 예상 소요 시간 경과 알림**

미션의 `estimatedMinutes` 시간이 지나면 Web Audio API(beep 사운드)와 앰버색 토스트 배너를 표시하여 시간 초과를 인지하게 한다. 타이머는 계속 진행되며(초과 시간 빨간색 표시), 완료 여부는 사용자가 결정한다.

---

### FR-8. AI 미션 재분해

**설명:** 사용자가 미션을 오래 수행하거나 부담스럽다고 느끼면 AI가 해당 미션을 더 작게 나눈다.

**버튼:** `AI에게 더 작게 나눠달라고 하기`

**예시**

기존 미션: `발표자료 목차 만들기`

재분해 결과:
1. 발표 제목 적기
2. 문제 정의 섹션 만들기
3. 해결 방법 섹션 만들기
4. 기대 효과 섹션 만들기

---

### FR-9. AI 피드백

**설명:** 하루 미션 완료 후 짧은 피드백을 제공한다.

**피드백 기준**
- 오늘 완료한 미션 수
- 평균 소요 시간
- 중단 여부
- 목표량 달성 여부

**예시**

> 오늘은 3개 미션을 완료했어요.  
> 처음 목표를 너무 크게 잡지 않고 잘게 나눠서 진행한 점이 좋아요.  
> 내일은 첫 미션을 5분짜리로 시작하면 더 쉽게 이어갈 수 있어요.

---

### FR-10. 커뮤니티 목업

**설명:** 커뮤니티는 MVP에서 실제 기능을 구현하지 않는다.

**화면 처리**

```
비슷한 목표를 가진 사람들과 함께하는 기능은 준비 중이에요.

예정 기능:
- 목표 유사도 기반 그룹 매칭
- 미션 인증 피드
- 응원 메시지
- 함께 달성 챌린지
```

---

## 11. 비기능 요구사항

| 항목 | 요구사항 |
|------|---------|
| 개발 속도 | 1~3일 내 배포 가능한 수준 |
| 반응형 | 모바일 + 데스크탑 반응형 (모바일: 하단 탭/상단 헤더, 데스크탑: 좌측 사이드바) |
| 저장 방식 | MVP는 localStorage, 추후 Supabase 마이그레이션 예정 |
| AI 호출 | OpenAI API (gpt-4o-mini), API Key 없을 시 mock data 자동 폴백 |
| 배포 | Vercel 자동 배포 (main 브랜치 push 시 트리거) |
| UI 스타일 | 밝고 친근한 게이미피케이션 톤 |
| 접근성 | 버튼 크기 충분히 크게, 텍스트 명확하게 |
| 안정성 | AI 응답 실패 시 기본 미션 템플릿 제공 |

---

## 12. 추천 기술 스택

| 영역 | 기술 |
|------|------|
| Frontend | Next.js 15 (App Router) |
| Styling | Tailwind CSS |
| UI Component | lucide-react (shadcn/ui 미사용) |
| State | React useState / useEffect |
| 저장 | localStorage → 추후 Supabase |
| AI API | OpenAI API (gpt-4o-mini) |
| 배포 | Vercel |
| 알림 | 내부 토스트 + Web Audio API (beep) |
| 아이콘 | lucide-react |

---

## 13. 데이터 모델

### Goal

```typescript
type Goal = {
  id: string;
  title: string;
  createdAt: string;
  deadline?: string;
  status: "active" | "completed" | "paused";
  dailyTargetCount: number;
  totalPoints: number;
};
```

### Chapter

```typescript
type Chapter = {
  id: string;
  goalId: string;
  title: string;
  order: number;
};
```

### Mission

```typescript
type Mission = {
  id: string;
  goalId: string;
  chapterId: string;
  title: string;
  description: string;
  estimatedMinutes: number;
  completionCriteria: string;
  status: "locked" | "available" | "in_progress" | "completed";
  order: number;
  startedAt?: string;
  completedAt?: string;
  durationSeconds?: number;
  pointsEarned?: number;
};
```

### UserProgress

```typescript
type UserProgress = {
  totalPoints: number;
  level: number;
  todayCompletedCount: number;
  dailyTargetCount: number;
  streak: number;
  lastActiveDate: string;
};
```

---

## 14. API 설계

### POST /api/generate-plan

**목적:** 사용자 목표를 받아 AI가 챕터와 미션으로 변환한다.

**Request**

```json
{
  "goal": "내일까지 창업 아이디어 발표자료 만들기",
  "dailyTargetCount": 3,
  "difficulty": "normal"
}
```

**Response**

```json
{
  "goalTitle": "창업 아이디어 발표자료 만들기",
  "chapters": [
    {
      "title": "아이디어 정리",
      "missions": [
        {
          "title": "서비스 한 줄 설명 작성하기",
          "description": "서비스가 누구의 어떤 문제를 해결하는지 한 문장으로 작성한다.",
          "estimatedMinutes": 5,
          "completionCriteria": "한 문장 설명 작성"
        }
      ]
    }
  ]
}
```

---

### POST /api/split-mission

**목적:** 부담스러운 미션을 더 작은 하위 미션으로 나눈다.

**Request**

```json
{
  "missionTitle": "발표자료 목차 만들기",
  "missionDescription": "발표 흐름에 맞게 목차를 구성한다."
}
```

**Response**

```json
{
  "missions": [
    {
      "title": "발표 제목 적기",
      "estimatedMinutes": 3
    },
    {
      "title": "문제 정의 섹션 적기",
      "estimatedMinutes": 5
    }
  ]
}
```

---

### POST /api/feedback

**목적:** 오늘 진행 결과를 바탕으로 짧은 AI 피드백 생성.

**Request**

```json
{
  "completedCount": 3,
  "dailyTargetCount": 3,
  "totalDurationMinutes": 42,
  "streak": 2
}
```

**Response**

```json
{
  "feedback": "오늘 목표량을 모두 완료했어요. 특히 첫 미션을 시작한 것이 가장 큰 성과예요."
}
```

---

## 15. LLM 프롬프트 초안

### 목표 세부화 프롬프트

```
너는 사용자의 목표를 실행 가능한 미션으로 나누는 목표 코치다.
사용자가 입력한 목표를 분석해서 2~4개의 챕터와 6~12개의 짧은 미션으로 나눠라.

규칙:
- 첫 미션은 5분 안에 할 수 있을 만큼 작아야 한다.
- 각 미션은 5~20분 내외로 수행 가능해야 한다.
- 미션은 구체적인 행동으로 시작해야 한다.
- "공부하기", "준비하기"처럼 모호하게 쓰지 말고 실제 행동으로 작성한다.
- 각 미션에는 description, estimatedMinutes, completionCriteria를 포함한다.
- 실패 부담을 줄이기 위해 쉬운 단계부터 배열한다.
- JSON 형식으로만 응답한다.

사용자 목표:
{{goal}}

출력 형식:
{
  "goalTitle": "",
  "chapters": [
    {
      "title": "",
      "missions": [
        {
          "title": "",
          "description": "",
          "estimatedMinutes": 5,
          "completionCriteria": ""
        }
      ]
    }
  ]
}
```

---

## 16. 포인트 계산 로직

```typescript
function calculateMissionPoints({
  completedTodayCount,
  dailyTargetCount,
  isReturnBonus,
}: {
  completedTodayCount: number;
  dailyTargetCount: number;
  isReturnBonus: boolean;
}) {
  let points = 0;
  if (completedTodayCount < dailyTargetCount) {
    points += 5;
  } else {
    points += 7;
  }
  const willReachDailyTarget = completedTodayCount + 1 === dailyTargetCount;
  if (willReachDailyTarget) {
    points += 30;
  }
  if (isReturnBonus) {
    points += 3;
  }
  return points;
}
```

---

## 17. MVP 화면별 개발 체크리스트

### 1단계 — 기본 화면
- [ ] Home 화면 제작
- [ ] 목표 입력창 제작
- [ ] AI 생성 버튼 제작
- [ ] 로딩 화면 제작

### 2단계 — AI 미션 생성
- [ ] `/api/generate-plan` 구현
- [ ] LLM JSON 응답 파싱
- [ ] 실패 시 기본 미션 템플릿 제공
- [ ] 생성된 미션 localStorage 저장

### 3단계 — 듀오링고식 맵 UI
- [ ] 챕터 카드 표시
- [ ] 미션 노드 표시
- [ ] 완료/진행 가능/잠김 상태 구분
- [ ] 다음 미션 해금 로직 구현

### 4단계 — 타이머
- [ ] 미션 시작 버튼
- [ ] 타이머 시작
- [ ] 일시정지
- [ ] 완료 버튼
- [ ] 완료 시간 저장

### 5단계 — 알림
- [ ] 미션 진행 30분 초과 시 모달
- [ ] 목표 생성 후 30분 미시작 시 토스트
- [ ] 브라우저 Notification 권한 요청
- [ ] 알림 거부 시 내부 토스트로 대체

### 6단계 — 점수/보상
- [ ] 기본 미션 완료 점수
- [ ] 하루 목표 달성 보너스
- [ ] 추가 미션 보상
- [ ] 총점 표시
- [ ] 레벨 표시

### 7단계 — 결과/피드백
- [ ] 오늘 완료 수 표시
- [ ] 오늘 획득 포인트 표시
- [ ] AI 피드백 생성
- [ ] 내일 추천 문구 표시

### 8단계 — 커뮤니티 목업
- [ ] 커뮤니티 탭 생성
- [ ] 준비 중 화면 표시
- [ ] 예정 기능 카드 표시

---

## 18. MVP 우선순위

### P0 — 반드시 구현

| 기능 | 이유 |
|------|------|
| 목표 입력 | 서비스 시작점 |
| AI 미션 생성 | 핵심 차별점 |
| 미션 맵 UI | 듀오링고식 핵심 UX |
| 미션 시작/완료 | 실행 앱의 본질 |
| 타이머 | 집중/진행감 제공 |
| 포인트 지급 | 게이미피케이션 핵심 |
| localStorage 저장 | 새로고침 유지 |

### P1 — 가능하면 구현

| 기능 | 이유 |
|------|------|
| 30분 알림 | 실행 유도 강화 |
| AI 미션 재분해 | 미션이 클 때 대응 |
| 하루 결과 화면 | 성취감 제공 |
| 레벨 시스템 | 재미 요소 강화 |

### P2 — 목업 처리

| 기능 | 이유 |
|------|------|
| 커뮤니티 | 개발 범위 큼 |
| 그룹 매칭 | 데이터 필요 |
| 인증 피드 | 후순위 |
| 친구 랭킹 | MVP 이후 |

---

## 19. 서비스 카피

**메인 문구**
> 해야 할 일 하나만 적어보세요.  
> AI가 바로 시작 가능한 미션으로 나눠드려요.

**보조 문구**
> 큰 목표를 작게 쪼개고,  
> 작은 성공을 점수로 쌓아보세요.

**버튼 문구**
- AI로 미션 만들기
- 미션 시작하기
- 완료했어요
- 다음 미션으로
- 더 작게 나눠줘
- 오늘 결과 보기

**알림 문구**
- 첫 미션은 5분이면 충분해요. 지금 시작해볼까요?
- 30분이 지났어요. 이 미션을 더 작게 나눠볼까요?

---

## 20. MVP 개발용 최종 요약

**만들 서비스**

목표 하나를 입력하면 AI가 짧은 미션으로 나누고, 사용자는 듀오링고식 단계 UI를 따라 미션을 수행하며 포인트를 얻는 목표 실행 웹앱.

**핵심 기능**

1. 목표 입력
2. AI 챕터/미션 생성
3. 듀오링고식 미션 맵
4. 미션 시작 타이머
5. 미션 완료 및 포인트 지급
6. 30분 리마인드/시간 알림
7. 하루 목표 달성 보너스
8. 커뮤니티는 준비 중 목업

**프로토타입에서 가장 중요한 장면**

```
사용자가 "발표자료 만들기" 입력
→ AI가 8개 미션으로 쪼갬
→ 미션들이 듀오링고처럼 단계별 노드로 보임
→ 첫 미션 시작
→ 타이머 작동
→ 완료 버튼
→ +5pt 획득
→ 다음 미션 해금
```

---

## 21. 바이브 코딩용 요청문

```
Next.js + Tailwind + shadcn/ui 기반으로 "나만의 AIm"이라는 목표 실행 웹앱 MVP를 만들어줘.
핵심 기능은 다음과 같아.
1. 사용자가 목표를 한 문장으로 입력한다.
2. OpenAI API를 호출해서 목표를 2~4개 챕터, 6~12개 미션으로 나눈다.
3. 각 미션은 title, description, estimatedMinutes, completionCriteria를 가진다.
4. 생성된 미션은 듀오링고처럼 세로형 단계 노드 UI로 보여준다.
5. 첫 미션만 available 상태이고, 나머지는 locked 상태다.
6. 미션을 완료하면 다음 미션이 해금된다.
7. 미션 시작 버튼을 누르면 스톱워치가 시작된다.
8. 미션 완료 버튼을 누르면 완료 시간과 포인트가 저장된다.
9. 하루 목표 미션 수는 기본 3개다.
10. 하루 목표 미션 수까지는 미션당 5pt, 하루 목표 달성 시 보너스 30pt, 초과 미션은 7pt를 지급한다.
11. 미션 진행 시간이 30분을 넘으면 "더 작게 나눠볼까요?" 모달을 띄운다.
12. 목표 생성 후 30분 동안 첫 미션을 시작하지 않으면 리마인드 토스트를 띄운다.
13. 데이터는 우선 localStorage에 저장한다.
14. 커뮤니티 탭은 실제 기능 없이 "준비 중" 목업 화면만 만든다.
15. 모바일 우선 반응형 UI로 만들어줘.
16. 전체 톤은 듀오링고처럼 밝고, 귀엽고, 성취감이 느껴지게 구성해줘.
```

---

## 22. 최종 MVP 이름/포지셔닝

**이름:** 나만의 AIm

**의미**
- AI + Aim
- 나만의 목표를 AI가 함께 조준해준다는 의미
- "I'm"과도 발음이 비슷해서 개인화된 실행 파트너 느낌을 줄 수 있음

**포지셔닝 문장**

> 나만의 AIm은 큰 목표를 작게 쪼개고, 작은 실행을 점수로 쌓아주는 AI 목표 실행 파트너입니다.
